package onl.tesseract.srp.domain.territory.guild.enum

enum class GuildJoinRequestResult {
    GUILD_NOT_FOUND,
    ALREADY_IN_GUILD,
    ALREADY_MEMBER,
    ALREADY_REQUESTED,
    FULL_MEMBERS,
    SUCCESS
}
